# 変更履歴

## 1.10

### 追加・改善

- アバターと背景の追加カメラ、RenderTextureへの描画、CustomRenderTextureの更新をGPU計測に追加しました。Forward・Deferred・VertexLitの描画経路と深度テクスチャを含みます。
- GPU時間は、カメラ描画・影生成・テクスチャ更新の重なる区間を1回だけ数えるようにしました。Sceneビュー・プレビュー用カメラ・同じ更新フレームのGameビュー再描画は除外します。
- 複数人の計測では、RenderTextureとそれを参照するマテリアル・アニメーションをアバターごとに分け、描画先の混在を防ぐようにしました。
- レポートの動作記録に、追加カメラ、物理設定、Cloth・Rigidbody・Joint・Contact・Raycastなどの有効状態を追加しました。
- Unityの結果画面・HTMLレポート・テキスト要約に、計測時のツールバージョンを表示します。JSONに保存した値を使うため、後から開き直しても計測時のバージョンを保持します。
- [計測対象と制約](COMPATIBILITY.md)を追加しました。Cloth・Joint・Raycastなどの動作条件、CPU値が表す範囲、VRChatクライアントとの違いを説明しています。

### 修正

- ビルドで生成されたRenderTextureなど、一部の未保存アセットの参照が計測用Prefabから抜ける問題を修正しました。
- Animatorの複製時に、Unityの内部整合性エラー（アサート）が発生する問題を修正しました。
- Clothなどのビルドで、アセットの内容が同じまま更新日時だけが変わった場合に、元データの変更と誤判定する問題を修正しました。
- RTを使うアバターの計測終了時に、`Releasing render texture that is set as Camera.targetTexture!`が発生する問題を修正しました。Av3Emulatorの非表示複製に残るカメラ参照を解除してから、計測用RTを解放します。

### 更新時の手順

1. 新しい`.unitypackage`を、使用中のプロジェクトへインポートします。
2. シーンを保存してUnityを再起動します。今回更新したGPU計測用DLLを読み込むために必要です。
3. カメラやRenderTextureを含むアバターは、ツールのアバタービルド欄から再ビルドします。元アバターがあるシーンを開いて実行してください。

計測プロファイルは引き続き使えます。再ビルド後にパラメータ名が変わった場合は、表示された項目を選び直してください。

GPUの計測範囲が変わるため、比較対象は同じ版・同じ条件で計測し直してください。旧形式のJSONは現在の結果画面では読み込めません。保存済みのHTMLレポートは、ファイルをブラウザーで直接開いて閲覧できます。

CPU値は引き続きメインスレッドの更新時間で、Cloth・物理演算のワーカーや描画スレッドなどの総CPU使用時間ではありません。Draw Calls・SetPassも、背景やEditorの描画が影響し得るフレーム全体の補助値です。
